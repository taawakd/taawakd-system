-- ══════════════════════════════════════════════════════════════════
-- supabase_migration_free_plan.sql
-- إضافة عمود تتبع التحليل المجاني الأول لكل مستخدم
-- يجب تشغيله مرة واحدة فقط في Supabase SQL Editor
-- ══════════════════════════════════════════════════════════════════

-- هل استخدم المستخدم التحليل المجاني الأول؟
-- false = لم يستخدمه بعد (التحليل القادم مفتوح بالكامل)
-- true  = استخدمه (جميع التحليلات القادمة مقفلة حتى يدفع)
ALTER TABLE profiles
  ADD COLUMN IF NOT EXISTS has_used_free_analysis BOOLEAN NOT NULL DEFAULT false;
